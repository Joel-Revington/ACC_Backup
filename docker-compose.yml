version: '3.8'

services:
  app:
    image: 339713031143.dkr.ecr.ap-south-1.amazonaws.com/acc_backup:latest
    container_name: acc_backup
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=production
    restart: always
